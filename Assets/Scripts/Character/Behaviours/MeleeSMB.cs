using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class MeleeSMB : BaseSMB
{
    public override void OnEnter(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
    {
        CharacterAbilityUser user = animator.GetComponent<CharacterAbilityUser>();
        user.GetAbility<MeleeAbility>().OnStart(user);
    }
}
*/