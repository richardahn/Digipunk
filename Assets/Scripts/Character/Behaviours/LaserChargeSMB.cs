using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class LaserChargeSMB : BaseSMB
{
    public override void OnEnter(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
    {
        CharacterAbilityUser user = animator.GetComponent<CharacterAbilityUser>();
        user.GetAbility<LaserAbility>().OnStart(user);
        user.GetAbility<LaserAbility>().OnChargeUpdate(user, animator);
    }
    public override void OnUpdate(Animator animator, AnimatorStateInfo stateInfo, int layerIndex)
    {
        CharacterAbilityUser user = animator.GetComponent<CharacterAbilityUser>();
        user.GetAbility<LaserAbility>().OnChargeUpdate(user, animator);
    }
}
*/